<article>
    <div>
        <a href="/post/{{id}}">{{title}}</a>
    </div>
    <div>
        {{post_text}}
    </div>
    <div>
        <strong>Posted by {{user.username}} on {{format_date created_at}}</strong>
    </div>
</article>