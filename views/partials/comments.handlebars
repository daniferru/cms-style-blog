<div>
    {{#each this}}
    <section>
        <div>
            {{user.username}} on {{format_date created_at}}
        </div>
        <div>
            {{comment_text}}

        </div>
        {{/each}}
    </section>
</div>